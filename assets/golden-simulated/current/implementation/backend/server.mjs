import {createHash,randomUUID} from 'node:crypto';import {createServer} from 'node:http';import {readFileSync} from 'node:fs';const web=readFileSync(new URL('../web/pages/submission/index.html',import.meta.url));const icon=readFileSync(new URL('../web/pages/submission/assets/icon.svg',import.meta.url));const APP_PORT=Number(process.env.PORT||4173);const INTEGRATION_SINK='/__integration_sink';const INTEGRATION_UNAVAILABLE='/__integration_unavailable';const PROVIDER_MAX_PARALLEL=16;const RESOURCE_STORE=new Map();const OPERATION_TOKENS=JSON.parse(process.env.VALIDATION_OPERATION_TOKENS||'{}');const readBody=request=>new Promise(resolve=>{const chunks=[];request.on('data',chunk=>chunks.push(chunk));request.on('end',()=>resolve(Buffer.concat(chunks)))});function multipartFiles(bytes,contentType){const boundary=contentType.match(/boundary=([^;]+)/i)?.[1];if(!boundary)return[bytes];return bytes.toString('binary').split('--'+boundary).slice(1,-1).flatMap(part=>{if(!/filename="/i.test(part))return[];const split=part.indexOf('\r\n\r\n');return split<0?[]:[Buffer.from(part.slice(split+4).replace(/\r\n$/,''),'binary')]})}async function mapPool(total,limit,worker){const out=new Array(total);let next=0;async function run(){while(next<total){const index=next++;out[index]=await worker(index)}}await Promise.all(Array.from({length:Math.min(limit,total)},()=>run()));return out}async function callProvider(url,inputs,challenge,timeoutMs){const operationToken=OPERATION_TOKENS["create-submission-submit-submission"];let res;try{res=await fetch(url,{method:'POST',headers:Object.assign({'content-type':'application/json'},challenge?{'x-validation-challenge':challenge}:{},operationToken?{'x-validation-operation-token':operationToken}:{}),body:JSON.stringify({inputs}),signal:timeoutMs?AbortSignal.timeout(timeoutMs):undefined})}catch(error){throw{kind:(error&&(error.name==='TimeoutError'||error.name==='AbortError'))?'timeout':'unavailable'}}if(res.status<200||res.status>=300)throw{kind:'unavailable'};const data=await res.json();if(!data||!data.externalResultId)throw{kind:'unavailable'};return data.externalResultId}export async function uploadResource(request,response){const bytes=await readBody(request);if(new URL(request.url,'http://local').searchParams.has('fail'))return json(response,422,{error:'INVALID_INPUT'});const files=multipartFiles(bytes,String(request.headers['content-type']||''));const assets=files.map(file=>{const checksum=createHash('sha256').update(file).digest('hex');const id='resource-'+checksum.slice(0,12);RESOURCE_STORE.set(id,file.toString('base64'));return{id,status:'available',checksum,file}});return json(response,201,{assetIds:assets.map(item=>item.id),assets:assets.map(({file,...item})=>item)})}export async function createSubmission(request,response){const query=new URL(request.url,'http://local').searchParams;const body=JSON.parse((await readBody(request)).toString());if(body['submission-title']==='brand-icon')return json(response,422,{error:'INVALID_INPUT'});const count=Number(body['result-quantity']);const resourceIds=body["upload-source-file-assetIds"];if(!Array.isArray(resourceIds)||!count||resourceIds.some(id=>!RESOURCE_STORE.has(id)))return json(response,422,{error:'INVALID_INPUT'});const inputs=resourceIds.map(id=>RESOURCE_STORE.get(id));const observer=process.env.EXTERNAL_OBSERVER_URL;if(observer){const challenge=process.env.VALIDATION_CHALLENGE_ID;const scenario=query.get('integrationScenario');if(scenario==='timeout'){try{await callProvider('http://127.0.0.1:'+APP_PORT+INTEGRATION_SINK,inputs,challenge,50)}catch(error){return json(response,504,{error:'PROVIDER_'+String(error.kind).toUpperCase()})}return json(response,504,{error:'PROVIDER_TIMEOUT'})}if(scenario==='unavailable'){try{await callProvider('http://127.0.0.1:'+APP_PORT+INTEGRATION_UNAVAILABLE,inputs,challenge)}catch(error){return json(response,503,{error:'PROVIDER_'+String(error.kind).toUpperCase()})}return json(response,503,{error:'PROVIDER_UNAVAILABLE'})}let items;try{items=await mapPool(count,PROVIDER_MAX_PARALLEL,()=>callProvider(observer,inputs,challenge,10000))}catch(error){return json(response,503,{error:'PROVIDER_'+String(error&&error.kind?error.kind:'unavailable').toUpperCase()})}const submissionId='submission-'+randomUUID().slice(0,8);return json(response,201,{submissionId,status:'succeeded',items})}const submissionId='submission-'+randomUUID().slice(0,8);return json(response,201,{submissionId,status:'succeeded',items:Array.from({length:count},(_,index)=>'/media/'+submissionId+'-'+(index+1)+'.txt')})}function json(response,status,value){response.writeHead(status,{'content-type':'application/json'});response.end(JSON.stringify(value))}createServer(async(request,response)=>{const path=new URL(request.url,'http://local').pathname;if(path==='/health')return json(response,200,{ok:true});if(path===INTEGRATION_SINK)return;if(path===INTEGRATION_UNAVAILABLE){response.writeHead(503,{'content-type':'application/json'});return response.end(JSON.stringify({error:'unavailable'}))}if(path==="/api/resources"&&request.method==='POST')return uploadResource(request,response);if(path==="/api/submissions"&&request.method==='POST')return createSubmission(request,response);if(path.startsWith('/media/')){response.writeHead(200,{'content-type':'text/plain; charset=utf-8'});return response.end('independent-media-item:'+path.slice(7))}if(path==='/assets/icon.svg'){response.writeHead(200,{'content-type':'image/svg+xml'});return response.end(icon)}response.writeHead(200,{'content-type':'text/html; charset=utf-8'});response.end(web)}).listen(APP_PORT);
