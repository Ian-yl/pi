import { createRequire } from 'node:module';import { mkdirSync,writeFileSync } from 'node:fs';const require=createRequire("/Users/ylzs/Documents/ylzs/project-implementation/package.json");const {chromium}=require('playwright');const challenge=JSON.parse(process.env.FRONTEND_UPLOAD_CHALLENGES)["upload-submission-upload-resource"];const browser=await chromium.launch({headless:true});const context=await browser.newContext();await context.tracing.start({screenshots:true,snapshots:true,sources:true});const page=await context.newPage();const cases=[];mkdirSync('evidence/frontend',{recursive:true});const capture=async(item,control)=>{const artifact='evidence/frontend/'+item.id+'.png';await page.screenshot({path:artifact,fullPage:true});await control.count();await control.isVisible();if(item.event!=='initial-state')await control.isEnabled();await page.content();item.artifacts=[artifact];item.status='passed';cases.push(item)};await page.goto(process.env.BASE_URL+'/submission');await page.waitForSelector('[data-clean-session="true"]');await capture({id:'initial-submission',capabilityId:'initial:submission',bindingId:'initial-submission',locator:'main',pageId:'submission',pageUrl:process.env.BASE_URL+'/submission',mode:'display-only',event:'initial-state'},page.locator('main'));await page.goto(process.env.BASE_URL+'/submission?uploadFailure=1');const upload=page.locator('#upload-input');await upload.setInputFiles(challenge.path);await page.waitForSelector('[data-upload-state] [data-state="failure"]');await capture({id:'browser-upload-failure',capabilityId:"cap-submission-upload-resource",bindingId:'binding-cap-submission-upload-resource',locator:"upload-input",pageUrl:process.env.BASE_URL+'/submission',mode:'reuse-control',event:'upload',expectedOutcome:'failure'},upload);await page.goto(process.env.BASE_URL+'/submission');const uploadSuccess=page.locator('#upload-input');await uploadSuccess.setInputFiles(challenge.path);await page.waitForSelector('[data-upload-state][data-result-status="success"] [data-result-binding-id]');await page.locator('[data-result-binding-id="binding-cap-submission-upload-resource-result-binding-cap-submission-upload-resource-assetids"]').evaluateAll(elements => elements.map(element => { const style = getComputedStyle(element); const rect = element.getBoundingClientRect(); return { visible: style.display !== 'none' && style.visibility !== 'hidden' && Number(style.opacity) > 0 && rect.width > 0 && rect.height > 0, tag: element.tagName.toLowerCase(), text: element.textContent.trim(), src: element.currentSrc || element.src || null, href: element.href || null, value: 'value' in element ? element.value : null }; }));await page.locator('[data-result-region-id="upload-panel"]:not([data-result-binding-id])').evaluateAll(elements => elements.map(element => { const style = getComputedStyle(element); const rect = element.getBoundingClientRect(); return { visible: style.display !== 'none' && style.visibility !== 'hidden' && Number(style.opacity) > 0 && rect.width > 0 && rect.height > 0, tag: element.tagName.toLowerCase(), text: element.textContent.trim(), src: element.currentSrc || element.src || null, href: element.href || null, value: 'value' in element ? element.value : null }; }));await page.locator('[data-display-binding-id="binding-cap-submission-upload-resource-upload-submission-upload-resource-response-assetids"]').evaluateAll(elements => elements.map(element => { const style = getComputedStyle(element); const rect = element.getBoundingClientRect(); return { visible: style.display !== 'none' && style.visibility !== 'hidden' && Number(style.opacity) > 0 && rect.width > 0 && rect.height > 0, tag: element.tagName.toLowerCase(), text: element.textContent.trim(), src: element.currentSrc || element.src || null, href: element.href || null, value: 'value' in element ? element.value : null }; }));await page.locator('[data-display-binding-id="binding-cap-submission-upload-resource-upload-submission-upload-resource-response-assets"]').evaluateAll(elements => elements.map(element => { const style = getComputedStyle(element); const rect = element.getBoundingClientRect(); return { visible: style.display !== 'none' && style.visibility !== 'hidden' && Number(style.opacity) > 0 && rect.width > 0 && rect.height > 0, tag: element.tagName.toLowerCase(), text: element.textContent.trim(), src: element.currentSrc || element.src || null, href: element.href || null, value: 'value' in element ? element.value : null }; }));await capture({id:'browser-upload',capabilityId:"cap-submission-upload-resource",bindingId:'binding-cap-submission-upload-resource',locator:"upload-input",pageUrl:process.env.BASE_URL+'/submission',mode:'reuse-control',event:'upload'},uploadSuccess);await page.goto(process.env.BASE_URL+'/submission');await page.locator('#title-input').fill("brand-icon");await page.locator('#category-select').fill("submit-button");await page.locator('#quantity-input').fill("2");const submitFailure=page.locator('#control-cap-submission-submit-submission');await submitFailure.click();await page.waitForSelector('[data-result-status="error"] [data-result-error="true"]');await capture({id:'browser-submit-failure',capabilityId:"cap-submission-submit-submission",bindingId:'binding-cap-submission-submit-submission',locator:"control-cap-submission-submit-submission",pageUrl:process.env.BASE_URL+'/submission',mode:'reuse-control',event:'click',expectedOutcome:'failure'},submitFailure);await page.goto(process.env.BASE_URL+'/submission');await page.locator('#title-input').fill("workspace");await page.locator('#category-select').fill("submit-button");await page.locator('#quantity-input').fill("3");const submitSuccess=page.locator('#control-cap-submission-submit-submission');await submitSuccess.click();await page.waitForSelector('[data-result-status="success"] [data-result-binding-id]');await page.locator('[data-display-binding-id="binding-cap-submission-submit-submission-create-submission-submit-submission-response-submissionid"]').evaluateAll(elements => elements.map(element => { const style = getComputedStyle(element); const rect = element.getBoundingClientRect(); return { visible: style.display !== 'none' && style.visibility !== 'hidden' && Number(style.opacity) > 0 && rect.width > 0 && rect.height > 0, tag: element.tagName.toLowerCase(), text: element.textContent.trim(), src: element.currentSrc || element.src || null, href: element.href || null, value: 'value' in element ? element.value : null }; }));await page.locator('[data-display-binding-id="binding-cap-submission-submit-submission-create-submission-submit-submission-response-status"]').evaluateAll(elements => elements.map(element => { const style = getComputedStyle(element); const rect = element.getBoundingClientRect(); return { visible: style.display !== 'none' && style.visibility !== 'hidden' && Number(style.opacity) > 0 && rect.width > 0 && rect.height > 0, tag: element.tagName.toLowerCase(), text: element.textContent.trim(), src: element.currentSrc || element.src || null, href: element.href || null, value: 'value' in element ? element.value : null }; }));await page.locator('[data-display-binding-id="binding-cap-submission-submit-submission-create-submission-submit-submission-response-items"]').evaluateAll(elements => elements.map(element => { const style = getComputedStyle(element); const rect = element.getBoundingClientRect(); return { visible: style.display !== 'none' && style.visibility !== 'hidden' && Number(style.opacity) > 0 && rect.width > 0 && rect.height > 0, tag: element.tagName.toLowerCase(), text: element.textContent.trim(), src: element.currentSrc || element.src || null, href: element.href || null, value: 'value' in element ? element.value : null }; }));await page.locator('[data-result-binding-id="binding-cap-submission-submit-submission-result-result-submit-submission"]').evaluateAll(elements => elements.map(element => { const style = getComputedStyle(element); const rect = element.getBoundingClientRect(); return { visible: style.display !== 'none' && style.visibility !== 'hidden' && Number(style.opacity) > 0 && rect.width > 0 && rect.height > 0, tag: element.tagName.toLowerCase(), text: element.textContent.trim(), src: element.currentSrc || element.src || null, href: element.href || null, value: 'value' in element ? element.value : null }; }));await page.locator('[data-result-region-id="result-panel"]:not([data-result-binding-id])').evaluateAll(elements => elements.map(element => { const style = getComputedStyle(element); const rect = element.getBoundingClientRect(); return { visible: style.display !== 'none' && style.visibility !== 'hidden' && Number(style.opacity) > 0 && rect.width > 0 && rect.height > 0, tag: element.tagName.toLowerCase(), text: element.textContent.trim(), src: element.currentSrc || element.src || null, href: element.href || null, value: 'value' in element ? element.value : null }; }));await page.waitForSelector('[data-detail-region][data-thumbs-loaded]',{state:'attached'});await capture({id:'browser-submit',capabilityId:"cap-submission-submit-submission",bindingId:'binding-cap-submission-submit-submission',locator:"control-cap-submission-submit-submission",pageUrl:process.env.BASE_URL+'/submission',mode:'reuse-control',event:'click'},submitSuccess);await page.goto(process.env.BASE_URL+'/submission');const planned=page.locator('#control-cap-submission-planned-export');await planned.click();await page.waitForSelector('[data-planned-surface="true"]');await capture({id:'browser-planned',capabilityId:"cap-submission-planned-export",bindingId:'binding-cap-submission-planned-export',locator:"control-cap-submission-planned-export",pageUrl:process.env.BASE_URL+'/submission',mode:'reuse-control',event:'click'},planned);const traceArtifact='evidence/frontend/playwright-trace.zip';await context.tracing.stop({path:traceArtifact});await browser.close();writeFileSync(process.env.FRONTEND_RAW_REPORT,JSON.stringify({schemaVersion:'1.0',engine:'playwright',traceArtifact,cases},null,2));
