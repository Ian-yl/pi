# Golden Simulated Flow

This fixture exercises the current chain from the immutable ai-restore sample Suite release through an approved functional-domain package, approved implementation handoff, implementation preparation, finalization, source locking, and simulated verification.

Regenerate the checked-in `current/` output:

```bash
node generate.mjs
```

CI invokes the same generator with a temporary `--output` directory.
