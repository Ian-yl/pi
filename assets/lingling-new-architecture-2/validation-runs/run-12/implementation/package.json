{
  "name": "lingling-image-site-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "npm --prefix web run build",
    "start": "node backend/server.js",
    "test": "node --test tests/*.test.mjs",
    "gate": "npm test && npm run build && node scripts/check-startup-json.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1"
  }
}
