{
  "schemaVersion": "1.0",
  "command": "npm start",
  "cwd": ".",
  "healthUrl": "http://127.0.0.1:${PORT}/api/pages/pg-1r/bootstrap",
  "requiredEnvironment": [
    "PORT",
    "OPENAI_BASE_URL",
    "OPENAI_API_KEY",
    "OPENAI_IMAGE_MODEL"
  ],
  "testMode": {
    "note": "Tests inject a deterministic provider adapter; production must not fabricate provider success."
  }
}
